spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev  # 开发环境
  application:
    name: microservicecloud-config-bjsole-dev
---
spring:
  profiles: test # 测试环境
  application:
    name: microservicecloud-config-bjsole-test

# 一定要用utf-8格式